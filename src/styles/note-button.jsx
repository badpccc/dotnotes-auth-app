const getNoteButtonStyles = (pressed) => ({
    backgroundColor: pressed ? "#EEE" : "#FFF",

    padding: 16,

    borderRadius: 12,

    marginBottom: 12,

    borderWidth: 1,

    borderColor: "#EEE",
});

export default getNoteButtonStyles;